Contains all the scripts I wrote for the campaign I ran (excluding ad-hoc files made for certain maps or puzzles).

Might upload a .txt file with macros I used at some point. After all, a lot of these files are contingent on some sort of macros to be able to use them cleanly.
Might upload images as well in case the image references in the files need to be changed to something you own on your own accounts.
